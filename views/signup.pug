extends layout
block title
    h2 Please type your user data

block content
    form(method="POST")
        div.group
            label(for="username") Username
            input(type="text" name="username" id="username" placeholder="User Name" value=data && data.username required)
            if errors && errors.username
                #usernameError.error= errors.username
        div.group
            label(for="password_hash") Password
            input(type="password" id="password_hash" name="password_hash" required)
            if errors && errors.password
                #usernameError.error= errors.password
        if errors && errors.auth
            #authError.error=errors.auth
        button(type="submit") Create my account